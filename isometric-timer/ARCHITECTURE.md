# Architecture

Native client-only web app: semantic HTML, CSS, vanilla JavaScript, Web Audio, Screen Wake Lock, Page Visibility, localStorage, and a minimal service worker for offline caching. No backend, framework, package manager, or build step.

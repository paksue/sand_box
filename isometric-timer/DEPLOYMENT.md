# Deployment

This folder is designed for static hosting from the repository. No build step is required. GitHub Pages (or any HTTPS static host) can serve the folder directly; HTTPS is required for Screen Wake Lock and service-worker behavior outside localhost.

[project]
name = "camscout"
version = "0.1.0"
description = "Authorized metadata-only camera exposure collector"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
camscout = "camscout.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
